export function workingFile(){
    const fruits = ['banana', 'apple', 'grapes', 'mangoo'];
    let output = '';

    for(let f of fruits){
        output += fruits +" "
    }
    console.log(output);
}